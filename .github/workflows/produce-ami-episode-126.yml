name: Produce AMI episode 126

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/produce-ami-episode-126.yml"

permissions:
  contents: read

jobs:
  produce:
    name: Episode 126
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      ELEVENLABS_API_KEY: ${{ secrets.ELEVENLABS_API_KEY }}
      ELEVENLABS_VOICE_ID: x1DOrfLtuvhgvW0jYdsS
      EPISODE_NUMBER: 126
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate API secret
        shell: bash
        run: test -n "$ELEVENLABS_API_KEY"

      - name: Install FFmpeg
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Reconstruct official music
        shell: bash
        run: |
          cat production/assets/music.b64.part-* | base64 --decode > production/assets/El_Diario_de_AMI.mp3

      - name: Produce and validate episode
        run: python3 production/produce_episode.py

      - name: Upload episode and QA
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: episodio-126-final
          path: |
            production/output/Episodio_126.mp3
            production/output/Episodio_126_QA.json
          if-no-files-found: error
          retention-days: 30
